const config = {
    botServer: "http://localhost:9000",
    /**[tick] */
    checkMessagesInterval: 10,
    timeZone: 9,  //UTC + n hours
    maxPlayers: 20  //Max players in the server
}

export default config
